replicaCount: 2

image:
  repository: flask-api
  pullPolicy: IfNotPresent
  tag: "latest"
  port: 5000

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
